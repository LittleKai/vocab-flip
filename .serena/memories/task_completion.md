# VocabFlip task completion
- Run `dart format` on edited Dart files.
- Run `flutter analyze` at least on edited paths; full-project analyze if feasible.
- If code changes affect behavior, validate the relevant flow manually or with tests where practical.
- Update `.claude/PROJECT_SUMMARY.md` after the session.